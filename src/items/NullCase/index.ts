import { ComponentOptions } from '../../editor/type';
import NullCase, { NullCaseProps } from './NullCase';

export const nullCaseOptions: ComponentOptions<NullCaseProps> = {
  component: NullCase,
  defaultSize: {
    width: 154,
    height: 44,
  },
  defaultData: {},
  detailPanel: [],
};
